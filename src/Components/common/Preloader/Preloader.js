import React from "react";
import loader from "../../../assets/images/ball-triangle.svg";

let Preloader = (props) => {
  return (
    <div>
      <img src={loader} />
    </div>
  );
};

export default Preloader;
